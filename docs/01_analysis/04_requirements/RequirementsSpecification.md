##### **폴더 기반 세션 구조**



로그인/회원가입 기능은 MVP 범위에서 제외한다. 시스템은 사이드바의 폴더 구조를 기준으로 개인 세션과 공유 세션을 구분한다.



1\. 사용자는 사이드바에서 폴더를 생성할 수 있어야 한다.

2\. 사용자는 폴더 안에 파일을 생성할 수 있어야 한다.

3\. 세션은 별도 로그인 계정이 아니라 폴더 단위로 구분한다.

4\. 개인 세션은 사용자가 직접 만든 폴더를 기준으로 한다.

5\. 공유 세션은 정해진 세션 이름을 가진 폴더를 기준으로 한다.

6\. 폴더는 개인 폴더, 공유 폴더 타입을 가져야 한다.

7\. 폴더 안의 파일은 세션 기록, 회의록, 주간 보고서, 코드 리뷰 결과 등 기능 산출물을 저장할 수 있어야 한다.



##### **임시 로그인 및 사용자 세션 구분 (localStorage 기반)**

시스템은 복잡한 로그인/회원가입 절차 없이, 원클릭 사용자 전환 버튼과 브라우저 세션 관리를 통해 데이터 영역을 격리한다.

1. 사용자는 UI에서 '일반사용자 1', '일반사용자 2', '관리자' 총 3가지 역할을 간편하게 스위칭하여 로그인할 수 있어야 한다.

2. 로그인된 사용자 정보(역할 ID 등)는 브라우저의 `localStorage`에 저장되어 세션이 유지되어야 한다.

3. 시스템은 `localStorage`에 기록된 활성 사용자 정보에 따라 사이드바 폴더 트리 및 파일 정보 데이터를 필터링하여 사용자별로 독립 격리된 세션을 구성한다.

4. 비밀번호를 이용한 폴더 잠금/접근 제어 요구사항(BCrypt/Argon2)은 MVP 단계에서 전면 제외(후순위화)하며, 역할 기반 세션 격리로 대체한다.

5. 공유 세션 폴더의 경우 별도의 입장 비밀번호 요구 없이, 고유한 공유 폴더 세션 이름과 참여자 별칭(Nickname)만 입력하면 즉시 진입할 수 있어야 한다.

6. 시스템은 공유 세션에 참여한 사용자들의 활동(작성, 수정 등)을 별칭 기준으로 구분하여 메타데이터에 기록한다.



##### **파일 생성 및 저장 방식**



1\. 사용자는 폴더 안에 파일을 생성할 수 있어야 한다.

2\. MVP에서 지원할 파일 형식은 Markdown(`.md`), HTML(`.html`), JSON(`.json`)으로 한다.

3\. Markdown 파일은 일반 기록, 회의록, 주간 보고서처럼 사람이 직접 읽고 수정하는 문서의 기본 저장 형식으로 사용한다.

4\. HTML 파일은 화면 미리보기 또는 출력용 렌더링 결과로 사용할 수 있다.

5\. JSON 파일은 AI 결과, 메타데이터, 구조화된 일정/마감일/리뷰 결과를 저장하는 형식으로 사용한다.

6\. 저장 방식은 라이브러리 선정 후 확정한다.

&#x20;   -> 후보: Markdown 중심 에디터, JSON 문서 모델 중심 에디터, 서버 사이드 Markdown/HTML 변환 라이브러리

7\. 같은 파일의 원본, 렌더링 결과, AI 분석 결과를 어떻게 연결할지 설계해야 한다.

&#x20;   -> 예: file_id 기준으로 content_markdown, content_html, content_json 또는 파일 버전 테이블 분리

8\. 파일 저장은 DB 저장과 로컬/오브젝트 스토리지 저장 중 어느 방식을 사용할지 분석 후 결정한다.

&#x20;   -> MVP는 PostgreSQL에 파일 메타데이터와 텍스트 본문을 저장하고, 대용량 첨부 파일은 별도 저장소로 분리하는 방향을 우선 검토



##### **시간 상담 및 기록 대시보드**



개인 세션에서 사용자가 음성 녹음 파일을 업로드하면, 시스템이 이를 텍스트로 변환하고 기록으로 저장하여 시간 기반 대시보드 형태로 시각화하는 기능을 제공한다.

시간 상담 및 기록 대시보드와 회의록은 앞단 입력 흐름을 공통으로 사용한다.

```text
음성 녹음/업로드
-> STT 변환
-> 원문 텍스트 저장
-> AI 목적 분기
   ├── 회의록 작성: 안건, 결정사항, 할 일, 담당자, 마감일 중심 요약
   └── 시간 기록: 시간대, 활동 종류, 집중도/중요도 가중치, 일간/주간 대시보드 생성
```

두 기능은 별도 업로드 기능을 중복 구현하지 않고, 같은 음성 처리 파이프라인을 사용한 뒤 결과물 타입만 다르게 저장한다.



###### **2-1 음성 파일 업로드 기능**



&#x20;**-> 세션에서 음성파일을 녹음할수있는 기능을 제공할지, 녹음 파일을 첨부할지 명확히 해야할듯**

1\. 사용자는 개인 세션에서 음성 파일을 업로드할 수 있어야 한다.

2\. 업로드 가능한 파일 형식은 다음을 지원해야 한다:

&#x20;  (예: mp3, wav, m4a) → 명확히 정의 필요

3\. 업로드 가능한 파일 크기 제한을 설정해야 한다. (예: 50MB 이하)

4\. 업로드 진행 상태(업로드 중, 완료, 실패)를 사용자에게 표시해야 한다.

5\. 업로드 실패 시 사용자에게 오류 메시지를 제공해야 한다.

6\. 업로드 또는 녹음된 음성은 회의록 작성과 시간 기록 생성에서 재사용할 수 있어야 한다.


###### **2-2 음성 → 텍스트 변환 기능**



1\. 업로드된 음성 파일은 자동으로 텍스트로 변환되어야 한다. 

&#x20;  -> 자동변환 혹은 버튼클릭?

2\. 변환 진행 상태를 사용자에게 표시해야 한다. (예: 변환 중, 완료)

3\. 변환 완료 후 텍스트 결과를 사용자에게 제공해야 한다.

4\. 변환 실패 시 재시도 혹은 실패 알림 기능을 제공해야 한다.

5\. 변환된 텍스트는 AI 목적 분기 전에 원문으로 저장되어야 한다.

6\. 사용자는 같은 원문 텍스트를 회의록으로 만들지, 시간 기록으로 만들지 선택할 수 있어야 한다.



###### **2-3 활동 기록 생성 및 저장 - 음성녹음 텍스트를  DB 에 저장할지 상의후 결정**



1\. 변환된 텍스트는 하나의 “활동 기록”으로 생성되어야 한다.

2\. 활동 기록에는 다음 정보가 포함되어야 한다:

&#x20;  - 텍스트 내용

&#x20;  - 업로드한 음성 파일 정보 (파일명, 경로 등)

&#x20;  - 생성 일시 (timestamp)

&#x20;  - 사용자 ID

&#x20;  - 세션 ID (개인 세션)

3\. 해당 데이터는 DB에 저장되어야 한다.

4\. 시간 기록은 활동 시간, 활동 유형, 중요도/집중도 가중치, 요약 내용을 포함해야 한다.

5\. 가중치 기준은 사용자가 수정할 수 있어야 하며, AI가 임의로 확정하지 않고 후보값으로 제안해야 한다.



###### **2-4 기록 조회 및 관리 기능 - 2-3이 되어야 가능할듯**



1\. 사용자는 자신의 개인 세션에서 저장된 기록 목록을 조회할 수 있어야 한다.

2\. 기록은 최신순 또는 시간순으로 정렬되어야 한다.

3\. 사용자는 특정 날짜 기준으로 기록을 필터링할 수 있어야 한다.



###### **2-5 대시보드 기능**



###### (1) 일간 대시보드

&#x09;1. 사용자는 특정 날짜의 활동 기록을 확인할 수 있어야 한다.

&#x09;2. 기록은 시간 순으로 시각화되어야 한다. (예: 타임라인 형태)



###### (2) 주간 대시보드

&#x09;1. 사용자는 주간 단위로 활동 요약 정보를 확인할 수 있어야 한다.

&#x09;2. 날짜별 기록 개수 또는 활동 요약을 제공해야 한다.



##### **접근 및 권한 요구사항**



1\. 해당 기능은 개인 폴더 또는 공유 세션 폴더 접근 조건을 통과한 사용자만 사용할 수 있어야 한다.

2\. 개인 세션의 데이터는 해당 사용자만 접근 가능해야 한다.



##### **예외 처리 요구사항**

1\. 지원하지 않는 파일 형식 업로드 시 오류 메시지를 표시해야 한다.

2\. 파일 크기 제한 초과 시 업로드를 제한해야 한다. - 첨부가 안되도록 막는게 맞을듯

3\. 텍스트 변환 실패 시 사용자에게 알림 및 재시도 기능을 제공해야 한다.

4\. 네트워크 오류 발생 시 업로드 재시도 기능을 제공해야 한다.



###### **추가 고려사항**

1\. 텍스트 수정 기능 (사용자가 변환 결과 편집 가능)

2\. 파일 삭제 시 기록 처리 정책 (같이 삭제 vs 유지)

3\. 변환 언어 설정 (한국어/영어 등)



#### **AI 코드 리뷰**



개발자가 개인세션에서 코드를 입력하면 AI가 코드의 효율성, 가독성, 유지보수성, 보안성 등을 분석하고 개선 방향과 리뷰 코멘트를 제공하는 기능이다. 개인세션은 사용자 본인만 접근 가능하며, 코드 리뷰 결과와 변경 이력을 저장할 수 있다.



**기능 요구사항**

1\. 사용자는 개인 코드 리뷰 세션을 생성할 수 있어야 한다.

2\. 사용자는 코드 입력창에 리뷰받을 코드를 입력할 수 있어야 한다.

3\. 사용자는 Java, JavaScript, Python 등 코드 언어를 선택할 수 있어야 한다.

4\. 사용자가 확인 버튼을 클릭하면 입력된 코드가 AI 리뷰 대상으로 전송되어야 한다.

5\. AI는 입력된 코드의 오류 가능성, 성능, 가독성, 중복 코드, 보안 위험 요소를 분석해야 한다.

6\. AI는 문제점과 개선 이유를 자연어 코멘트로 제공해야 한다.

7\. AI는 필요 시 더 나은 코드 예시를 함께 제안해야 한다.

8\. 리뷰 결과는 세션 단위로 저장되어야 한다.

9\. 사용자가 리뷰 후 코드를 수정하면 이전 코드와 수정된 코드를 비교할 수 있어야 한다.

10\. 사용자는 개인세션 내에서 이전 리뷰 내역을 조회할 수 있어야 한다.

11\. 입력된 코드는 개인세션 외부 사용자에게 노출되지 않아야 한다.

12\. 코드가 비어 있거나 AI 응답 실패 시 적절한 안내 메시지를 표시해야 한다.



**비기능 요구사항**

1\. 일반적인 코드 입력 기준 리뷰 결과는 일정 시간 내에 제공되어야 한다.

2\. 개인세션 데이터는 폴더 비밀번호를 통과한 사용자만 접근 가능해야 한다.

3\. GitHub 연동, PR diff 리뷰, 공유세션 코드 리뷰로 확장 가능해야 한다.

4\. 코드 입력창과 리뷰 결과 영역은 한 화면에서 확인 가능해야 한다.

5\. 리뷰 기록은 세션, 사용자, 작성 시간 기준으로 관리되어야 한다.



1\. 사용자는 개인세션에서 코드를 입력하고 리뷰 요청을 할 수 있다. 

2\. 시스템은 입력된 코드를 AI 모델에 전달하여 효율성, 가독성, 보안성, 유지보수성 측면에서 분석하고 개선

&#x20;  코멘트와 수정 예시 코드를 제공한다. 

3\. 리뷰 결과는 개인세션 단위로 저장되며, 사용자는 이전 리뷰 기록을 조회할 수 있다.



##### **공유세션 ------------------------------------------------------------------------------**



##### **회의록**



공유세션 참여자가 회의 녹음 파일을 업로드하거나 직접 녹음하면, 시스템은 공통 음성 처리 파이프라인으로 텍스트를 만들고 AI를 통해 회의 내용을 요약한다. 생성된 회의록은 공유세션 내 참여자들이 확인할 수 있으며, Excel 또는 한글 파일로 출력할 수 있다.



**기능 요구사항**

1\. 세션 이름과 필요한 경우 비밀번호를 알고 있는 사용자는 공유세션에 참여할 수 있어야 한다.

2\. 사용자는 회의 녹음 파일을 업로드할 수 있어야 한다.

3\. 사용자는 공유세션 내에서 회의 내용을 직접 녹음할 수 있어야 한다.

4\. 시스템은 지원 가능한 파일 형식과 용량을 검증해야 한다.

5\. 시스템은 업로드 또는 녹음된 음성 데이터를 텍스트로 변환해야 한다.

6\. 사용자는 변환된 회의 텍스트를 확인할 수 있어야 한다.

7\. AI는 회의 내용을 핵심 안건, 결정 사항, 할 일, 담당자 중심으로 요약해야 한다.

8\. 생성된 회의록은 공유세션 단위로 저장되어야 한다.

9\. 공유세션 참여자는 생성된 회의록을 조회할 수 있어야 한다.

10\. 사용자는 회의록 결과를 Excel 파일로 다운로드할 수 있어야 한다.

11\. 사용자는 회의록 결과를 한글 문서 형식으로 다운로드할 수 있어야 한다.

12\. 음성 변환 실패, AI 요약 실패, 파일 출력 실패 시 안내 메시지를 표시해야 한다.



**비기능 요구사항**

1\. 공유세션 입장 조건을 통과한 사용자만 회의록을 조회할 수 있어야 한다.

2\. 업로드된 음성 파일과 변환 텍스트는 외부에 노출되지 않아야 한다.

3\. 음성 길이에 따라 처리 시간이 길어질 수 있으므로 진행 상태를 표시해야 한다.

4\. 추후 회의록 수정, 댓글, 담당자 알림 기능으로 확장 가능해야 한다.

5\. 녹음, 업로드, 요약, 다운로드 흐름이 한 화면에서 이해 가능해야 한다.



1\. 사용자는 공유세션에서 회의 녹음 파일을 업로드하거나 직접 녹음할 수 있다. 

2\. 시스템은 음성 데이터를 텍스트로 변환하고, 변환된 텍스트를 AI 모델에 전달하여 회의 내용을 요약한다. 

3\. 요약 결과는 주요 안건, 결정 사항, 할 일, 담당자, 마감일 항목으로 구성되며 공유세션 참여자들이 

&#x20;  조회할 수 있다. 

4\. 사용자는 생성된 회의록을 Excel 또는 한글 파일로 다운로드할 수 있다.



##### **AI 데드라인 추출**



회의록 요약 결과에서 일정 및 마감 정보를 AI가 자동으로 추출하고, 이를 캘린더 형태로 시각화하여 사용자에게 제공하는 기능이다. 사용자는 메인 페이지에서 주요 데드라인을 한눈에 확인할 수 있으며, 마감 임박 시 알림을 받을 수 있다.



**기능 요구사항**

1\. 시스템은 AI 요약 결과에서 일정, 마감일, 작업 내용을 자동으로 추출해야 한다.

2\. 추출된 데이터는 제목, 설명, 담당자, 시작일, 마감일 형태로 구조화되어야 한다.

3\. 추출된 일정은 사용자 캘린더에 자동으로 등록되어야 한다.

4\. 사용자는 캘린더 화면에서 일정 및 데드라인을 조회할 수 있어야 한다.

5\. 메인페이지 진입 시 임박한 데드라인 및 주요 일정이 표시되어야 한다.

6\. 사용자는 특정 일정 클릭 시 상세 내용을 확인할 수 있어야 한다.

7\. 사용자는 자동 생성된 일정 정보를 수정할 수 있어야 한다.

8\. 사용자는 필요 없는 일정 데이터를 삭제할 수 있어야 한다.

9\. 시스템은 데드라인 임박 시 사용자에게 알림을 제공해야 한다.

10\. 사용자는 알림 시간(예: 1일 전, 3시간 전 등)을 설정할 수 있어야 한다.

11\. 데드라인 전까지 해당 작업 결과를 AI가 검토할 수 있는 기능을 제공해야 한다.

12\. 일정 추출 실패, 날짜 파싱 오류 발생 시 사용자에게 안내 메시지를 표시해야 한다.



**비기능 요구사항**

1\. AI는 날짜, 시간, 작업 내용을 최대한 정확하게 추출해야 한다.

2\. 회의록 생성 이후 일정 추출은 빠르게 처리되어야 한다.

3\. 캘린더 UI는 직관적으로 확인 가능해야 한다.

4\. 알림은 설정된 시간에 정확하게 전달되어야 한다.

5\. 일정 데이터는 해당 사용자 또는 공유세션 참여자만 접근 가능해야 한다.



**확장 기능**

1\. 담당자 자동 매핑 (회의 참여자 기반)

2\. 이메일 알림 연동

3\. 일정 우선순위 표시 (긴급 / 중요)

4\. AI 일정 충돌 감지

5\. 일정 완료 체크 + 진행률 관리



1\. 시스템은 회의록 요약 결과로부터 일정 및 마감 정보를 AI를 통해 자동 추출해야 한다. 

2\. 추출된 정보는 캘린더 형식으로 자동 등록되며, 사용자는 메인페이지에서 임박한 데드라인을 

&#x20;  확인할 수 있어야 한다. 

3\. 시스템은 마감 일정 이전에 사용자에게 알림을 제공하며, 사용자는 일정 정보를 수정 및 관리할 수 

&#x20;  있어야 한다.



##### **주간 보고서 작성**



개인세션과 공유세션에서 생성된 작업 기록, 회의록 요약, 시간 기록, 데드라인 데이터를 AI가 종합 분석하여 주간 보고서를 자동 작성하는 기능이다. 사용자는 생성된 보고서를 확인하고 수정한 뒤 저장하거나 파일로 출력할 수 있다.



**기능 요구사항**

1\. 사용자는 주간 보고서를 생성할 기간을 선택할 수 있어야 한다.

2\. 시스템은 선택한 기간의 개인세션 기록, 공유세션 회의록, 시간 기록, 데드라인 데이터를 수집해야 한다.

3\. AI는 수집된 데이터를 바탕으로 주요 업무, 진행 상황, 완료 항목, 미완료 항목을 분석해야 한다.

4\. AI는 분석 결과를 바탕으로 주간 보고서를 자동 작성해야 한다.

5\. 보고서는 주요 업무, 회의 요약, 작업 진행률, 완료 업무, 미완료 업무, 다음 주 계획 항목을 포함해야 한다.

6\. 사용자는 생성된 보고서를 화면에서 미리 확인할 수 있어야 한다.

7\. 사용자는 AI가 작성한 보고서 내용을 직접 수정할 수 있어야 한다.

8\. 수정 또는 확정된 보고서는 개인세션 단위로 저장되어야 한다.

9\. 사용자는 보고서를 Excel 또는 한글 파일 형식으로 출력할 수 있어야 한다.

10\. 사용자는 이전에 작성한 주간 보고서를 조회할 수 있어야 한다.

11\. 보고서 생성에 필요한 데이터가 부족할 경우 안내 메시지를 표시해야 한다.

12\. 사용자는 동일 기간의 보고서를 다시 생성할 수 있어야 한다.



**비기능 요구사항**

1\. AI는 실제 기록된 데이터를 기반으로 보고서를 작성해야 한다.

2\. 개인세션 보고서는 작성자 본인만 조회할 수 있어야 한다.

3\. 보고서 생성, 수정, 저장, 출력 흐름이 직관적이어야 한다.

4\. 보고서 생성 중 진행 상태를 사용자에게 표시해야 한다.

5\. 추후 월간 보고서, 프로젝트 보고서로 확장 가능해야 한다.

6\. 데이터가 없는 항목은 AI가 임의로 작성하지 않고 “해당 없음”으로 표시해야 한다.



1\. 사용자는 개인세션에서 주간 보고서 작성 기간을 선택할 수 있다. 

2\. 시스템은 선택한 기간 내 개인세션 기록, 공유세션 회의록, 시간 기록, 데드라인 데이터를 수집하고 

&#x20;  AI를 통해 주간 보고서 초안을 생성한다. 

3\. 사용자는 생성된 보고서를 확인하고 수정할 수 있으며, 최종 보고서를 저장하거나 Excel 또는 

&#x20;  한글 파일로 출력할 수 있다.
